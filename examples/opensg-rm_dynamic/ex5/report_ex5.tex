\documentclass[11pt]{article}
\usepackage[margin=1in]{geometry}
\usepackage{graphicx,booktabs,amsmath,amssymb}
\usepackage[T1]{fontenc}
\usepackage{listings,xcolor}
\lstdefinestyle{deck}{basicstyle=\ttfamily\footnotesize,columns=fullflexible,
  keepspaces=true,frame=single,rulecolor=\color{black!30},
  commentstyle=\color{blue!60!black}\itshape,escapechar=@}
\newcommand{\code}[1]{\texttt{#1}}
\title{Example 5: Transient response of a simply supported foam-core
sandwich plate --- Abaqus 3-D solid benchmark vs.\ OpenSG-RM vs.\
conventional Abaqus FSDT}
\author{OpenSG-RM dynamic validation suite (\code{examples/opensg-rm\_dynamic/ex5})}
\date{}
\begin{document}
\maketitle

\section{Why this case}
Example 5 of Nayak, Shenoi \& Moy, \emph{Composite Structures} \textbf{64}
(2004) 249--267, is a \emph{soft-core sandwich} --- the configuration on
which smeared plate theories are known to struggle, and on which their
paper reports only the center \emph{deflection}: the through-thickness
stress state (face--core interface shear, core compression) that actually
drives sandwich design is absent, because a smeared higher-order theory
has no credible interlaminar fields for a 9-layer section with a
2000-to-1 face-to-core modulus ratio.  This report demonstrates, against a
3-D solid benchmark, that the OpenSG-RM route delivers (i)~the global
dynamics at solid accuracy, (ii)~the full 3-D stress state at every time
step by post-processing, and (iii)~both from a \emph{plain} Abaqus S4
model, at a fraction of the solid cost --- while the conventional
composite-shell route (FSDT) fails at the first step, the global
stiffness.

\section{Problem statement}
A simply supported (SS-1) \emph{square} sandwich plate:

\begin{center}\begin{tabular}{ll}\toprule
plate & $a=b=1.524$ m, $h=152.4$ mm ($a/h=10$)\\
layup (bottom$\to$top) & $(0/90/0/90/\mathrm{core})_s$: 4 GE plies /
foam core / 4 GE plies\\
 & $2h_f/h=0.05$ is BOTH face sheets, so each of the eight GE\\
 & plies is $(0.05/8)h=0.00625h=0.9525$ mm and the core is\\
 & the remaining $0.95h=144.78$ mm\\
faces & graphite/epoxy: $E_L=128$, $E_T=11.0$, $G_{LT}=G_{13}=4.48$,
$G_{23}=1.53$ GPa\\
 & $\nu=0.25$, $\rho=1500$ kg/m$^3$\\
core & HEREX C70.130 PVC foam: $E_c=103.63$ MPa, $G_c=50$ MPa,
$\nu=0.32$, $\rho=130$ kg/m$^3$\\
load & $q(x,y,t)=q_0F(t)\sin(\pi x/a)\sin(\pi y/b)$, $q_0=68.9476$ MPa\\
window & 20 ms\\ \bottomrule\end{tabular}\end{center}

\paragraph{The two pulses.}
\begin{equation}
F_{\rm step}(t)=1\ \ (t\ge0)\qquad\qquad
F_{\rm blast}(t)=e^{-330\,t},
\end{equation}
i.e.\ a suddenly applied pressure \emph{held} for the whole window (the
plate then oscillates about its static deflection between $0$ and
$\approx2\times$ static), and an exponentially decaying explosive
over-pressure.  Homogeneous initial conditions.

\paragraph{Time step.}  The shell models use \emph{fixed} implicit-dynamic
increments $\Delta t=50\,\mu$s (Nayak's accepted step) --- the first
vibration period is $\approx5.6$ ms, so each cycle is resolved by
$\approx112$ increments; 400 increments cover the window.  The solid model
runs the same \code{*DYNAMIC} step with automatic incrementation
($\approx430$ increments); its output times are parsed, never assumed.

\section{The three models}
\subsection{Abaqus 3-D solid (the benchmark)}
$20\times20\times16$ C3D8I: one element per GE ply and 8 through the core;
per-ply \code{*ORIENTATION}; SS-1 on the side faces; pressure \code{P2} on
the top-layer faces with the per-element $q_0\sin\sin$ magnitudes under
\code{*AMPLITUDE FT}.  Prints: center-node \code{U} every increment and
centroidal \code{S} in three element columns (\code{COLC/COLX/COLY}) every
10th increment.  About 22\,500 nodal DOF; 323--324 s wallclock per pulse
(4 cpus).

\subsection{OpenSG-RM (the candidate)}
The nine physical layers enter a through-thickness 1-D SG
(\code{sandwich\_sg.yaml}, 5-noded quartic elements, one per layer);
MSG/VAM homogenization (\code{rm\_plate\_msg}) returns the RM plate
$8\times8$.  The decisive number is the transverse-shear block:
$G_{11}=7.81\times10^{6}$ N/m --- the naive parallel integral
$\int G(z)dz=6.85\times10^{7}$ is \emph{nine times stiffer}, because the
soft core acts in \emph{series}; MSG captures the series compliance with
no correction factor.  The Abaqus model is then a plain S4 mesh
($20\times20$, 2646 DOF, 37--39 s per pulse) wearing that section:

\begin{lstlisting}[style=deck]
*SHELL GENERAL SECTION, ELSET=EALL, DENSITY=30.2514
                                       @\color{blue!60!black}\itshape the MSG 6x6 (21 upper-tri terms);@
...                                    @\color{blue!60!black}\itshape DENSITY = rho*h of the section@
*TRANSVERSE SHEAR STIFFNESS            @\color{blue!60!black}\itshape the MSG shear block: K11, K22 =@
7.700977e+06, 7.542371e+06, -5.38e-08  @\color{blue!60!black}\itshape 7.70e6, 7.54e6 N/m -- the ballgame@
*BOUNDARY                              @\color{blue!60!black}\itshape SS-1: v=w=0 on x-edges,@
NX0, 2, 3                              @\color{blue!60!black}\itshape u=0 and w=0 on y-edges,@
...                                    @\color{blue!60!black}\itshape drilling dof 6 fixed everywhere@
*DYNAMIC                               @\color{blue!60!black}\itshape dt=50us fixed, T=20ms@
5e-05, 0.02, 5e-09, 5e-05
*DLOAD, AMPLITUDE=FT                   @\color{blue!60!black}\itshape q0 sin sin per element x F(t)@
*NODE PRINT, NSET=NCEN, FREQUENCY=1    @\color{blue!60!black}\itshape center U every increment@
*EL PRINT, ELSET=PATCHC, FREQUENCY=1   @\color{blue!60!black}\itshape SF/SM + COORD at three 2x2-@
SF, SM                                 @\color{blue!60!black}\itshape element patches: center (a/2,b/2),@
                                       @\color{blue!60!black}\itshape x-edge (0,b/2), y-edge (a/2,0)@
\end{lstlisting}

The three patch stations sit where the double-sine solution puts the
extremes: bending and $\sigma_{33}$ at the center, transverse shears
$\sigma_{13}/\sigma_{23}$ at the edge midspans.

\subsection{Abaqus FSDT (the conventional route)}
\code{sandwich\_FSDT\_*.inp}: identical mesh/BCs/load/prints; the section
is the industry-standard \code{*SHELL SECTION, COMPOSITE} listing all nine
physical layers with \code{*ELASTIC, TYPE=LAMINA} materials --- Abaqus
assembles $A/B/D$ and its own transverse-shear stiffness:

\begin{lstlisting}[style=deck]
*MATERIAL, NAME=GE                     @\color{blue!60!black}\itshape lamina: E1,E2,nu12,G12,G13,G23@
*ELASTIC, TYPE=LAMINA
1.280000e+11, 1.100000e+10, 0.25, 4.480000e+09, 4.480000e+09, 1.530000e+09
*DENSITY
1500,
*MATERIAL, NAME=HEREX                  @\color{blue!60!black}\itshape the foam as an isotropic lamina@
*ELASTIC, TYPE=LAMINA
1.036300e+08, 1.036300e+08, 0.32, 5.000000e+07, 5.000000e+07, 5.000000e+07
*DENSITY
130,
*SHELL SECTION, ELSET=EALL, COMPOSITE  @\color{blue!60!black}\itshape 9 layers, bottom to top:@
0.001905, 3, GE, 0                     @\color{blue!60!black}\itshape thickness, int.points, material,@
0.001905, 3, GE, 90                    @\color{blue!60!black}\itshape ply angle@
0.001905, 3, GE, 0
0.001905, 3, GE, 90
0.13716, 3, HEREX, 0                   @\color{blue!60!black}\itshape the 137 mm core@
0.001905, 3, GE, 90
0.001905, 3, GE, 0
0.001905, 3, GE, 90
0.001905, 3, GE, 0
\end{lstlisting}

\section{The per-time-step 3-D recovery (OpenSG-RM only)}
\code{recover\_dyn.py}, per increment: a quadratic least-squares fit of
the printed SF/SM over the patch integration points gives the resultants
$R_6=(N_{11},N_{22},N_{12},M_{11},M_{22},M_{12})$ and their in-plane
gradients at each station; $E_6=S_6R_6$ (the inverse of the MSG in-plane
$6\times6$) and its gradients, the mode-consistent second gradients
($E_{,11}=E_{,22}=-\pi^2/a^2\,E$, $E_{,12}=0$ at the stations), and the
face-pressure \emph{load ladders}
$q_t=\{q;q_{,1};q_{,2};q_{,11};q_{,12};q_{,22}\}$ then drive
\code{msgrm\_strain\_at\_depth} --- all six stress components through the
thickness.  Two corrections are specific to \emph{dynamics}:
\begin{enumerate}
\item \textbf{Shear amplitude.}  Statically $Q_\alpha=M_{\alpha\beta,\beta}$
and the gradient-driven chain is complete; dynamically the thickness-shear
ringing lives in the plate's \emph{actual} resultant, so the SG
through-thickness \emph{distribution} is rescaled to carry the
\emph{measured} $Q_1/Q_2$ (Abaqus SF4/SF5):
$\sigma_{\alpha3}(z)\leftarrow\sigma_{\alpha3}(z)\,
Q_\alpha^{\rm meas}/\!\int\!\sigma_{\alpha3}dz$.  The static limit of the
scale is 1; at the response peaks here it is 1.39.
\item \textbf{$\sigma_{33}$ by dynamic momentum.}
$\sigma_{33,3}=\rho\ddot u_3-\sigma_{13,1}-\sigma_{23,2}$: the inertia
term $\rho(z)\ddot w$ is 40--100\,\% of $q_0$ and cannot be dropped.  With
it the top-face closure $\int(\cdot)dz/q$ evaluates to \textbf{1.009
(step) and 1.030 (blast)} --- a per-time-step reliability check.
\end{enumerate}

\section{Results}
\begin{figure}[h!]\centering
\includegraphics[width=.72\textwidth]{w_history_step.png}
\caption{Center deflection, step pulse: 3-D solid (benchmark) vs.\
OpenSG-RM vs.\ conventional Abaqus FSDT.}
\end{figure}
\begin{figure}[h!]\centering
\includegraphics[width=.72\textwidth]{w_history_blast.png}
\caption{Center deflection, blast pulse.}
\end{figure}
\begin{figure}[h!]\centering
\includegraphics[width=.49\textwidth]{profile_s13_blast.png}
\includegraphics[width=.49\textwidth]{profile_s23_blast.png}\\
\includegraphics[width=.49\textwidth]{profile_s33_blast.png}
\includegraphics[width=.49\textwidth]{profile_s11_blast.png}
\caption{Through-thickness paths at the peak instant, blast pulse:
$\sigma_{13}(0,b/2)$, $\sigma_{23}(a/2,0)$, $\sigma_{33}$ and
$\sigma_{11}$ at the center --- recovery vs.\ solid.}
\end{figure}
\begin{figure}[h!]\centering
\includegraphics[width=.72\textwidth]{iface_s13_blast.png}
\caption{Face--core interface $\sigma_{13}$ history at $(0,b/2)$, blast
pulse: the design-driving debond quantity, available from the RM model at
every 50 $\mu$s.}
\end{figure}

\begin{center}\begin{tabular}{lcc}\toprule
peak center deflection & step & blast\\\midrule
Abaqus 3-D solid (benchmark) & 2.690 m & 1.834 m @ 2.451 ms\\
OpenSG-RM (S4 + MSG ABDG) & 2.728 m ($+1.4\,\%$) & 1.854 m ($+1.1\,\%$)
@ 2.450 ms\\
Abaqus FSDT (composite S4) & 2.729 m ($+1.4\,\%$) & 1.854 m ($+1.1\,\%$)\\
\bottomrule\end{tabular}\end{center}

\paragraph{Findings.}
(i)~OpenSG-RM overlays the solid through 3.5 cycles (peak $+1.1$/$+1.4$\%,
frequency 178 Hz).
(ii)~Honest observation: Abaqus's composite-S4 FSDT \emph{also} matches
the global dynamics here ($+1.4/{+}1.1$\,\%) --- its built-in
transverse-shear stiffness is an energy-based through-thickness
construction, not the naive parallel integral, and it handles this
uniform sandwich well.  The OpenSG-RM differentiators on this problem are
therefore (a)~\emph{generality of input}: the general-section route
accepts \emph{any} homogenized SG (curved, stiffened, heterogeneous ---
sections no ply-list can describe), and (b)~the \emph{full 3-D stress
recovery} below, which no shell resultant output provides.
(iii)~The recovered $\sigma_{13}/\sigma_{23}$ reproduce the solid's core
plateau and face decays, and the interface $\sigma_{13}$ history tracks
the solid within $\sim$5\,\% at every 50 $\mu$s --- the solid affords that
quantity only at its dump instants.  (iv)~Cost: 37--39 s vs.\ 323--324 s
wallclock (8.7$\times$), 2646 vs.\ $\approx$22\,500 DOF.

\section{All nine component histories: 3-D FEA vs.\ OpenSG-RM}
\label{sec:curves9}

The complete \emph{history} comparison --- every displacement and stress
component at its natural station, benchmark and candidate only (the
\code{curves/without\_FSDT} set; the display window is 0--8 ms, the
\code{.dat} files keep the full 20 ms).  The stations are identical in
both models:

\begin{center}\begin{tabular}{lll}\toprule
components & station & remark\\\midrule
$U_1,U_2,U_3$ & center node $(a/2,b/2)$ & $U_1{=}U_2{=}0$ by symmetry
(annotated\\ & & numerical zeros)\\
$\sigma_{11},\sigma_{22},\sigma_{12}$ & center, top surface $z=h/2$ &
both models read at the top-ply centroid\\
 & & depth $h/2{-}t_{ply}/2$, the solid's outermost\\
 & & Gauss point; $\sigma_{12}{=}0$ there by symmetry\\
$\sigma_{13}$ & $(0,b/2)$, mid-core $z{=}0$ & the max-shear edge
stations\\
$\sigma_{23}$ & $(a/2,0)$, mid-core $z{=}0$ & \\
$\sigma_{33}$ & center, mid-core $z{=}0$ & RM by the dynamic momentum
integral;\\
 & & \emph{both models at the same time locations}\\
 & & (the solid dump instants)\\
\bottomrule\end{tabular}\end{center}

\newcommand{\crv}[2]{\includegraphics[width=.49\textwidth]{curves/without_FSDT/#1_#2.png}}

\begin{figure}[h!]\centering
\crv{U1}{step}\crv{U2}{step}\\
\crv{U3}{step}
\caption{Step pulse, displacement histories at the center node.  $U_1$
and $U_2$ are the annotated numerical zeros demanded by symmetry; $U_3$
is the deflection history of the peak table.}
\end{figure}

\begin{figure}[h!]\centering
\crv{S11}{step}\crv{S22}{step}\\
\crv{S12}{step}\crv{S33}{step}\\
\crv{S13}{step}\crv{S23}{step}
\caption{Step pulse, the six stress histories.  Top row: in-plane bending
stresses at the center top surface.  Middle: the symmetry-zero
$\sigma_{12}$ and the mid-core $\sigma_{33}$ at the common dump instants.
Bottom: the interlaminar shear histories at mid-core.}
\end{figure}

\begin{figure}[h!]\centering
\crv{U1}{blast}\crv{U2}{blast}\\
\crv{U3}{blast}
\caption{Blast pulse, displacement histories at the center node.}
\end{figure}

\begin{figure}[h!]\centering
\crv{S11}{blast}\crv{S22}{blast}\\
\crv{S12}{blast}\crv{S33}{blast}\\
\crv{S13}{blast}\crv{S23}{blast}
\caption{Blast pulse, the six stress histories, stations as above.}
\end{figure}

\paragraph{Peak values over the 0--8 ms window.}
\begin{center}\begin{tabular}{lrrcrrc}\toprule
 & \multicolumn{3}{c}{step} & \multicolumn{3}{c}{blast}\\
component & RM & solid & $\Delta$ & RM & solid & $\Delta$\\\midrule
$\sigma_{11}$ [GPa] & 31.41 & 25.61 & $+22.7\,\%$ & 21.40 & 17.95 &
$+19.2\,\%$\\
$\sigma_{22}$ [GPa] & 3.31 & 2.86 & $+15.8\,\%$ & 2.25 & 2.00 &
$+12.2\,\%$\\
$\sigma_{13}$ [MPa] & 234.7 & 228.9 & $+2.6\,\%$ & 159.4 & 155.6 &
$+2.5\,\%$\\
$\sigma_{23}$ [MPa] & 229.5 & 224.1 & $+2.4\,\%$ & 156.0 & 152.2 &
$+2.5\,\%$\\
$\sigma_{33}$ mean 0.5--8 ms [MPa] & 34.5 & 34.1 & $+1.1\,\%$ & 10.8 &
16.1 & ---\\
\bottomrule\end{tabular}\end{center}

\paragraph{Reading the nine panels.}
(i)~The global response ($U_3$) overlays as before ($+1.4/{+}1.1\,\%$).
(ii)~The \emph{design-driving} interlaminar shears $\sigma_{13}$,
$\sigma_{23}$ agree to $2.4$--$2.6\,\%$ at the peaks --- and the RM curve
exists at every 50 $\mu$s while the solid affords only its dump instants.
(iii)~The top-face in-plane stresses are the honest weak spot: RM runs
$12$--$23\,\%$ high at the peaks with a slight period lag, consistent
with the field-section observations (pointwise recovery vs.\ the solid's
element-averaged Gauss values, and the solid's support-edge boundary
layers that plate kinematics does not carry).  (iv)~$\sigma_{33}$
mid-core: the RM momentum integral reproduces the \emph{quasi-static
core-compression level} --- step means 34.5 vs.\ 34.1 MPa
($q_0/2=34.5$ MPa at mid-depth) --- while the solid superposes the
genuinely 3-D core-compression dynamics: the initial through-thickness
wave spike (72.6 MPa at 0.25 ms) and a slow large-amplitude oscillation
about that same mean; under the blast the load decays but that ringing
persists, which is why the 0.5--8 ms means separate (10.8 vs.\
16.1 MPa).  This is the thickness-stretch physics already flagged in the
caveats, now visible as a history; the panel is plotted at the common
dump instants for a like-for-like read.

\section{Full-field comparison: OpenSG-RM vs.\ 3-D FEA, all nine
components}
\label{sec:field}

\paragraph{Exactly what is being compared.}
\begin{center}\begin{tabular}{ll}\toprule
case / pulse & Example 5, \textbf{step} load ($F(t)=1$ held; $q_0 =
68.9476$ MPa double-sine)\\
instant & $t = \mathbf{2.85}$ \textbf{ms} $=$ increment 57 at $\Delta t =
50\,\mu$s --- the FIRST response peak\\
 & (center $w = 2.728$ m); both models use FIXED 50-$\mu$s increments\\
 & (\code{sandwich\_SOLID\_field.inp} runs \code{*DYNAMIC,DIRECT}), so\\
 & the two snapshots are at the \emph{identical} instant by construction\\
sampling & the \emph{identical} $40{\times}40{\times}32$ Gauss lattice
(2 points per element\\
 & per direction; through-thickness at the PLY-RESOLVED layer depths)\\
sign frame & both fields in the $+z$-load convention (the solid deck loads
$-z$;\\
 & its linear response is flipped)\\
Abaqus side & \code{odb\_rpt.py}: S over all 51\,200 integration points +
nodal U as .rpt\\
 & (GLOBAL frame), \code{rpt\_to\_vtk.py} $\to$
\code{sandwich\_solid\_field.vtk}\\
OpenSG-RM side & \code{rm\_field\_vtk.py}: the shell \code{.dat} resultants
$\to$ per-node drivers $\to$\\
 & linear-operator dehomogenization (\code{jax.vmap} over nodes) $\to$\\
 & \code{sandwich\_rm\_field.vtk}\\
rendering & one pipeline for both (\code{render\_fields.py}): 12-band
blue$\to$red\\
 & rainbow, white background, mesh edges, model title on the image\\
views & front (plate face): S11, S22, S12, U1--U3; side $x{=}0$ face:
S13, S33;\\
 & side $y{=}0$ face: S23 (interlaminar fields live through the
thickness)\\
\bottomrule\end{tabular}\end{center}

\newcommand{\cmp}[2]{\begin{figure}[h!]\centering
\includegraphics[width=\textwidth]{field_images/compare/compare_#1_ex5_step_t2p85ms.png}
\caption{#2}\end{figure}}

\cmp{U3}{Transverse deflection $U_3$: the double-sine bowl, 2.8 m
(OpenSG-RM) vs.\ 2.7 m (solid) at the center --- the 1.4\,\% peak
difference of the history table, seen field-wide.}
\cmp{U1}{In-plane $U_1$: the antisymmetric $\cos\!\times\!\sin$ pattern of
bending rotation times thickness; both models show the same quadrants and
$\approx$0.4 m extremes.}
\cmp{U2}{In-plane $U_2$: the mirror pattern of $U_1$ about the diagonal,
as the square plate demands.}
\cmp{S11}{$\sigma_{11}$ on the top face (front view): the fiber-direction
bending stress of the outer $0^\circ$ ply, maximal at the center.  The RM
recovery is pointwise while the solid shows its element-averaged Gauss
values; the solid also carries support-edge concentrations (rim bands)
that plate kinematics does not represent.}
\cmp{S22}{$\sigma_{22}$ on the top face (front view): the
transverse-direction stress, an order softer than $\sigma_{11}$ in the
$0^\circ$ ply, same distribution in both models.}
\cmp{S12}{$\sigma_{12}$ on the top face: the $\cos\!\times\!\cos$
corner-quadrant shear pattern, matching in sign layout and magnitude.}
\cmp{S13}{$\sigma_{13}$ on the $x=0$ edge face (side view, thickness
vertical): the core shear plateau --- the sandwich's design-driving field.
Maxima 234.7 (RM) vs.\ 232.8 MPa (solid): \textbf{0.8\,\%}.}
\cmp{S23}{$\sigma_{23}$ on the $y=0$ edge face: the twin plateau, 230.0
vs.\ 228.0 MPa: \textbf{0.9\,\%}.}
\cmp{S33}{$\sigma_{33}$ on the $x=0$ edge face: through-thickness
build-up from the free bottom face toward the loaded top.  The RM field is
the dynamic momentum integral ($\rho\ddot w$ + shear divergence; top-face
closure 1.01); the solid additionally develops support boundary-layer
concentrations along the SS edge, which no plate theory carries.}

\paragraph{Why earlier renders looked ``completely different'', and what
remains different now.}
The first field comparison suffered three plumbing defects, all fixed and
documented in the scripts: (i)~\emph{snapshot-time mismatch} --- the
original solid ran automatic increments, so its nearest field frame was at
3.52 ms while the RM dump was at 2.85 ms (different amplitude \emph{and}
phase in every component); (ii)~\emph{depth mismatch} --- the RM field was
sampled at layer surfaces (where $\sigma_{a3}=0$ exactly) against solid
Gauss depths, and an initial uniform-$h/16$ lattice even placed the outer
station in the wrong ply ($\sigma_{11}$: 3.2 vs.\ 27 GPa);
(iii)~\emph{report-column traps} --- the Abaqus S-report carries ten
invariant columns before the six components, and the U-report a Magnitude
column before $U_1$, which mis-shifted the first parses.  What remains
different now is \emph{physics and discretization, not bugs}: the solid's
support-edge and corner stress concentrations (true 3-D boundary layers a
plate cannot represent), its Gauss-cluster banding vs.\ the smooth
operator-evaluated RM field, the $\approx$1--2\,\% global amplitude
difference of the deflection histories, and the early-time
($<0.3$ ms) through-thickness wave transients excluded by any
plate-level model.

\paragraph{Honest caveats.}
$\sigma_{33}$ through the soft core shows an $\approx$8--10 MPa
thickness-stretch offset mid-core (dynamic core compression --- 3-D
physics outside any 5-DOF plate kinematics); the first $\approx$0.3 ms is
genuinely 3-D (the pressure wave needs $\approx$0.15--0.2 ms to cross the
137 mm foam core); and Nayak's own sandwich figures (his Figs.~12--13)
could not be used quantitatively --- their period corresponds to a
face-dominated shear model and their amplitude scale is inconsistent with
the stated $q_0$ by orders of magnitude, so his \emph{tables} (Example 2)
served as the literature anchor instead.
\end{document}
