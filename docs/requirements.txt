# Docs build (GitHub Pages). Notebooks are committed pre-executed (nb_execution_mode="off"),
# so the heavy compute stack (jax, pypardiso, basix) is NOT needed to build the site.
sphinx>=7.2
sphinx-book-theme>=1.1
myst-nb>=1.1
sphinx-design>=0.5
sphinx-copybutton>=0.5
